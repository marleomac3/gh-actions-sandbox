name: Hello World Workflow

on:
  workflow_dispatch:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello world and all who inhabit it!!"
